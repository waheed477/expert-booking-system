Please implement these backend fixes for the Expert Booking App:

1. Install missing packages:
npm install joi express-validator dotenv cors

2. Create .env file in backend folder:
PORT=5000
DATABASE_URL=postgresql://postgres:password@localhost:5432/expert_booking
NODE_ENV=development
CLIENT_URL=http://localhost:3000

3. Create middleware/errorHandler.js:
const errorHandler = (err, req, res, next) => {
  console.error(err.stack);

  if (err.name === 'ValidationError') {
    return res.status(400).json({
      success: false,
      error: 'Validation Error',
      details: err.errors
    });
  }

  if (err.code === '23505') { // PostgreSQL unique violation
    return res.status(409).json({
      success: false,
      error: 'Duplicate entry',
      message: 'This slot is already booked'
    });
  }

  res.status(err.status || 500).json({
    success: false,
    error: err.message || 'Internal Server Error'
  });
};

module.exports = errorHandler;

4. Create validation/bookingValidation.js:
const Joi = require('joi');

const bookingSchema = Joi.object({
  expertId: Joi.string().uuid().required(),
  userName: Joi.string().min(3).max(50).required(),
  userEmail: Joi.string().email().required(),
  userPhone: Joi.string().pattern(/^[0-9]{10}$/).required(),
  bookingDate: Joi.date().iso().required(),
  timeSlot: Joi.string().pattern(/^([0-1]?[0-9]|2[0-3]):[0-5][0-9] (AM|PM)$/).required(),
  notes: Joi.string().max(500).optional()
});

const validateBooking = (req, res, next) => {
  const { error } = bookingSchema.validate(req.body);
  if (error) {
    return res.status(400).json({
      success: false,
      error: error.details[0].message
    });
  }
  next();
};

module.exports = { validateBooking };

5. Update routes/experts.js - Add pagination to GET endpoint:
Add this code in the GET / route:

const page = parseInt(req.query.page) || 1;
const limit = parseInt(req.query.limit) || 6;
const offset = (page - 1) * limit;
const category = req.query.category;
const search = req.query.search;

let query = db.select().from(experts);

if (category) {
  query = query.where(eq(experts.category, category));
}

if (search) {
  query = query.where(sql`LOWER(name) LIKE LOWER(${`%${search}%`})`);
}

const total = await query;
const results = await query.limit(limit).offset(offset);

res.json({
  data: results,
  pagination: {
    page,
    limit,
    total: total.length,
    pages: Math.ceil(total.length / limit)
  }
});

6. Update routes/bookings.js - Add status update and cancel endpoints:

// Update booking status
router.patch('/:id/status', async (req, res, next) => {
  try {
    const { id } = req.params;
    const { status } = req.body;

    const validStatuses = ['pending', 'confirmed', 'completed', 'cancelled'];
    if (!validStatuses.includes(status)) {
      return res.status(400).json({ error: 'Invalid status' });
    }

    const [updatedBooking] = await db
      .update(bookings)
      .set({ status, updatedAt: new Date() })
      .where(eq(bookings.id, id))
      .returning();

    if (!updatedBooking) {
      return res.status(404).json({ error: 'Booking not found' });
    }

    // Emit WebSocket event
    if (req.app.get('wss')) {
      req.app.get('wss').clients.forEach(client => {
        if (client.readyState === 1) {
          client.send(JSON.stringify({
            type: 'BOOKING_UPDATED',
            data: updatedBooking
          }));
        }
      });
    }

    res.json(updatedBooking);
  } catch (error) {
    next(error);
  }
});

// Cancel booking
router.post('/:id/cancel', async (req, res, next) => {
  try {
    const { id } = req.params;
    
    const [booking] = await db
      .update(bookings)
      .set({ status: 'cancelled', updatedAt: new Date() })
      .where(and(
        eq(bookings.id, id),
        eq(bookings.status, 'pending')
      ))
      .returning();

    if (!booking) {
      return res.status(400).json({ 
        error: 'Cannot cancel this booking' 
      });
    }

    // Free up the slot
    await db
      .update(expertSlots)
      .set({ isBooked: false, bookedBy: null })
      .where(and(
        eq(expertSlots.expertId, booking.expertId),
        eq(expertSlots.date, booking.bookingDate),
        eq(expertSlots.time, booking.timeSlot)
      ));

    // Emit WebSocket event for slot freed
    if (req.app.get('wss')) {
      req.app.get('wss').clients.forEach(client => {
        if (client.readyState === 1) {
          client.send(JSON.stringify({
            type: 'SLOT_FREED',
            data: {
              expertId: booking.expertId,
              date: booking.bookingDate,
              timeSlot: booking.timeSlot
            }
          }));
        }
      });
    }

    res.json({ success: true, booking });
  } catch (error) {
    next(error);
  }
});

7. Update server.js to use error handler (add at the bottom before export):
app.use(errorHandler);